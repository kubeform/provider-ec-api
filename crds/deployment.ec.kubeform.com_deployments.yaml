apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  creationTimestamp: null
  labels:
    app.kubernetes.io/name: ec.kubeform.com
    app.kubernetes.io/part-of: kubeform.com
  name: deployments.deployment.ec.kubeform.com
spec:
  group: deployment.ec.kubeform.com
  names:
    kind: Deployment
    listKind: DeploymentList
    plural: deployments
    singular: deployment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            properties:
              providerRef:
                description: LocalObjectReference contains enough information to let
                  you locate the referenced object inside the same namespace.
                properties:
                  name:
                    description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      TODO: Add other useful fields. apiVersion, kind, uid?'
                    type: string
                type: object
              resource:
                properties:
                  alias:
                    description: Optional deployment alias that affects the format
                      of the resource URLs
                    type: string
                  apm:
                    description: Optional APM resource definition
                    properties:
                      config:
                        description: Optionally define the Apm configuration options
                          for the APM Server
                        properties:
                          debugEnabled:
                            description: Optionally enable debug mode for APM servers
                              - defaults to false
                            type: boolean
                          userSettingsJSON:
                            description: An arbitrary JSON object allowing (non-admin)
                              cluster owners to set their parameters (only one of
                              this and 'user_settings_yaml' is allowed), provided
                              they are on the whitelist ('user_settings_whitelist')
                              and not on the blacklist ('user_settings_blacklist').
                              (This field together with 'user_settings_override*'
                              and 'system_settings' defines the total set of resource
                              settings)
                            type: string
                          userSettingsOverrideJSON:
                            description: An arbitrary JSON object allowing ECE admins
                              owners to set clusters' parameters (only one of this
                              and 'user_settings_override_yaml' is allowed), ie in
                              addition to the documented 'system_settings'. (This
                              field together with 'system_settings' and 'user_settings*'
                              defines the total set of resource settings)
                            type: string
                          userSettingsOverrideYaml:
                            description: An arbitrary YAML object allowing (non-admin)
                              cluster owners to set their parameters (only one of
                              this and 'user_settings_json' is allowed), provided
                              they are on the whitelist ('user_settings_whitelist')
                              and not on the blacklist ('user_settings_blacklist').
                              (These field together with 'user_settings_override*'
                              and 'system_settings' defines the total set of resource
                              settings)
                            type: string
                          userSettingsYaml:
                            description: An arbitrary YAML object allowing ECE admins
                              owners to set clusters' parameters (only one of this
                              and 'user_settings_override_json' is allowed), ie in
                              addition to the documented 'system_settings'. (This
                              field together with 'system_settings' and 'user_settings*'
                              defines the total set of resource settings)
                            type: string
                        type: object
                      elasticsearchClusterRefID:
                        type: string
                      httpEndpoint:
                        type: string
                      httpsEndpoint:
                        type: string
                      refID:
                        type: string
                      region:
                        type: string
                      resourceID:
                        type: string
                      topology:
                        items:
                          properties:
                            instanceConfigurationID:
                              type: string
                            size:
                              type: string
                            sizeResource:
                              description: Optional size type, defaults to "memory".
                              type: string
                            zoneCount:
                              format: int64
                              type: integer
                          type: object
                        type: array
                    type: object
                  deploymentTemplateID:
                    description: Required Deployment Template identifier to create
                      the deployment from
                    type: string
                  elasticsearch:
                    description: Required Elasticsearch resource definition
                    properties:
                      autoscale:
                        description: Enable or disable autoscaling. Defaults to the
                          setting coming from the deployment template. Accepted values
                          are "true" or "false".
                        type: string
                      cloudID:
                        description: The encoded Elasticsearch credentials to use
                          in Beats or Logstash
                        type: string
                      config:
                        description: Optional Elasticsearch settings which will be
                          applied to all topologies unless overridden on the topology
                          element
                        properties:
                          plugins:
                            description: List of Elasticsearch supported plugins,
                              which vary from version to version. Check the Stack
                              Pack version to see which plugins are supported for
                              each version. This is currently only available from
                              the UI and [ecctl](https://www.elastic.co/guide/en/ecctl/master/ecctl_stack_list.html)
                            items:
                              type: string
                            minItems: 1
                            type: array
                          userSettingsJSON:
                            description: JSON-formatted user level "elasticsearch.yml"
                              setting overrides
                            type: string
                          userSettingsOverrideJSON:
                            description: JSON-formatted admin (ECE) level "elasticsearch.yml"
                              setting overrides
                            type: string
                          userSettingsOverrideYaml:
                            description: YAML-formatted admin (ECE) level "elasticsearch.yml"
                              setting overrides
                            type: string
                          userSettingsYaml:
                            description: YAML-formatted user level "elasticsearch.yml"
                              setting overrides
                            type: string
                        type: object
                      extension:
                        description: Optional Elasticsearch extensions such as custom
                          bundles or plugins.
                        items:
                          properties:
                            name:
                              description: Extension name.
                              type: string
                            type:
                              description: Extension type, only `bundle` or `plugin`
                                are supported.
                              type: string
                            url:
                              description: Bundle or plugin URL, the extension URL
                                can be obtained from the `ec_deployment_extension.<name>.url`
                                attribute or the API and cannot be a random HTTP address
                                that is hosted elsewhere.
                              type: string
                            version:
                              description: Elasticsearch compatibility version. Bundles
                                should specify major or minor versions with wildcards,
                                such as `7.*` or `*` but **plugins must use full version
                                notation down to the patch level**, such as `7.10.1`
                                and wildcards are not allowed.
                              type: string
                          required:
                          - name
                          - type
                          - url
                          - version
                          type: object
                        minItems: 1
                        type: array
                      httpEndpoint:
                        description: The Elasticsearch resource HTTP endpoint
                        type: string
                      httpsEndpoint:
                        description: The Elasticsearch resource HTTPs endpoint
                        type: string
                      refID:
                        description: Optional ref_id to set on the Elasticsearch resource
                        type: string
                      region:
                        description: The Elasticsearch resource region
                        type: string
                      remoteCluster:
                        description: Optional Elasticsearch remote clusters to configure
                          for the Elasticsearch resource, can be set multiple times
                        items:
                          properties:
                            alias:
                              description: Alias for this Cross Cluster Search binding
                              type: string
                            deploymentID:
                              description: Remote deployment ID
                              type: string
                            refID:
                              description: Remote elasticsearch "ref_id", it is best
                                left to the default value
                              type: string
                            skipUnavailable:
                              description: If true, skip the cluster during search
                                when disconnected
                              type: boolean
                          required:
                          - deploymentID
                          type: object
                        minItems: 1
                        type: array
                      resourceID:
                        description: The Elasticsearch resource unique identifier
                        type: string
                      snapshotSource:
                        description: Optional snapshot source settings. Restore data
                          from a snapshot of another deployment.
                        properties:
                          snapshotName:
                            description: Name of the snapshot to restore. Use '__latest_success__'
                              to get the most recent successful snapshot.
                            type: string
                          sourceElasticsearchClusterID:
                            description: ID of the Elasticsearch cluster that will
                              be used as the source of the snapshot
                            type: string
                        required:
                        - sourceElasticsearchClusterID
                        type: object
                      topology:
                        description: Optional topology element which must be set once
                          but can be set multiple times to compose complex topologies
                        items:
                          properties:
                            ID:
                              description: Required topology ID from the deployment
                                template
                              type: string
                            autoscaling:
                              description: Optional Elasticsearch autoscaling settings,
                                such a maximum and minimum size and resources.
                              properties:
                                maxSize:
                                  description: Maximum size value for the maximum
                                    autoscaling setting.
                                  type: string
                                maxSizeResource:
                                  description: Maximum resource type for the maximum
                                    autoscaling setting.
                                  type: string
                                minSize:
                                  description: Minimum size value for the minimum
                                    autoscaling setting.
                                  type: string
                                minSizeResource:
                                  description: Minimum resource type for the minimum
                                    autoscaling setting.
                                  type: string
                                policyOverrideJSON:
                                  description: Computed policy overrides set directly
                                    via the API or other clients.
                                  type: string
                              type: object
                            instanceConfigurationID:
                              description: Computed Instance Configuration ID of the
                                topology element
                              type: string
                            nodeRoles:
                              description: The computed list of node roles for the
                                current topology element
                              items:
                                type: string
                              type: array
                            nodeTypeData:
                              description: The node type for the Elasticsearch Topology
                                element (data node)
                              type: string
                            nodeTypeIngest:
                              description: The node type for the Elasticsearch Topology
                                element (ingest node)
                              type: string
                            nodeTypeMaster:
                              description: The node type for the Elasticsearch Topology
                                element (master node)
                              type: string
                            nodeTypeMl:
                              description: The node type for the Elasticsearch Topology
                                element (machine learning node)
                              type: string
                            size:
                              description: Optional amount of memory per node in the
                                "<size in GB>g" notation
                              type: string
                            sizeResource:
                              description: Optional size type, defaults to "memory".
                              type: string
                            zoneCount:
                              description: Optional number of zones that the Elasticsearch
                                cluster will span. This is used to set HA
                              format: int64
                              type: integer
                          required:
                          - ID
                          type: object
                        minItems: 1
                        type: array
                      trustAccount:
                        description: Optional Elasticsearch account trust settings.
                        items:
                          properties:
                            accountID:
                              description: The ID of the Account.
                              type: string
                            trustAll:
                              description: If true, all clusters in this account will
                                by default be trusted and the `trust_allowlist` is
                                ignored.
                              type: boolean
                            trustAllowlist:
                              description: The list of clusters to trust. Only used
                                when `trust_all` is false.
                              items:
                                type: string
                              type: array
                          required:
                          - accountID
                          - trustAll
                          type: object
                        type: array
                      trustExternal:
                        description: Optional Elasticsearch external trust settings.
                        items:
                          properties:
                            relationshipID:
                              description: The ID of the external trust relationship.
                              type: string
                            trustAll:
                              description: If true, all clusters in this account will
                                by default be trusted and the `trust_allowlist` is
                                ignored.
                              type: boolean
                            trustAllowlist:
                              description: The list of clusters to trust. Only used
                                when `trust_all` is false.
                              items:
                                type: string
                              type: array
                          required:
                          - relationshipID
                          - trustAll
                          type: object
                        type: array
                    type: object
                  elasticsearchUsername:
                    description: Computed username obtained upon creating the Elasticsearch
                      resource
                    type: string
                  enterpriseSearch:
                    description: Optional Enterprise Search resource definition
                    properties:
                      config:
                        description: Optionally define the Enterprise Search configuration
                          options for the Enterprise Search Server
                        properties:
                          userSettingsJSON:
                            description: An arbitrary JSON object allowing (non-admin)
                              cluster owners to set their parameters (only one of
                              this and 'user_settings_yaml' is allowed), provided
                              they are on the whitelist ('user_settings_whitelist')
                              and not on the blacklist ('user_settings_blacklist').
                              (This field together with 'user_settings_override*'
                              and 'system_settings' defines the total set of resource
                              settings)
                            type: string
                          userSettingsOverrideJSON:
                            description: An arbitrary JSON object allowing ECE admins
                              owners to set clusters' parameters (only one of this
                              and 'user_settings_override_yaml' is allowed), ie in
                              addition to the documented 'system_settings'. (This
                              field together with 'system_settings' and 'user_settings*'
                              defines the total set of resource settings)
                            type: string
                          userSettingsOverrideYaml:
                            description: An arbitrary YAML object allowing (non-admin)
                              cluster owners to set their parameters (only one of
                              this and 'user_settings_json' is allowed), provided
                              they are on the whitelist ('user_settings_whitelist')
                              and not on the blacklist ('user_settings_blacklist').
                              (These field together with 'user_settings_override*'
                              and 'system_settings' defines the total set of resource
                              settings)
                            type: string
                          userSettingsYaml:
                            description: An arbitrary YAML object allowing ECE admins
                              owners to set clusters' parameters (only one of this
                              and 'user_settings_override_json' is allowed), ie in
                              addition to the documented 'system_settings'. (This
                              field together with 'system_settings' and 'user_settings*'
                              defines the total set of resource settings)
                            type: string
                        type: object
                      elasticsearchClusterRefID:
                        type: string
                      httpEndpoint:
                        type: string
                      httpsEndpoint:
                        type: string
                      refID:
                        type: string
                      region:
                        type: string
                      resourceID:
                        type: string
                      topology:
                        items:
                          properties:
                            instanceConfigurationID:
                              type: string
                            nodeTypeAppserver:
                              type: boolean
                            nodeTypeConnector:
                              type: boolean
                            nodeTypeWorker:
                              type: boolean
                            size:
                              type: string
                            sizeResource:
                              description: Optional size type, defaults to "memory".
                              type: string
                            zoneCount:
                              format: int64
                              type: integer
                          type: object
                        type: array
                    type: object
                  id:
                    type: string
                  kibana:
                    description: Optional Kibana resource definition
                    properties:
                      config:
                        description: Optionally define the Kibana configuration options
                          for the Kibana Server
                        properties:
                          userSettingsJSON:
                            description: An arbitrary JSON object allowing (non-admin)
                              cluster owners to set their parameters (only one of
                              this and 'user_settings_yaml' is allowed), provided
                              they are on the whitelist ('user_settings_whitelist')
                              and not on the blacklist ('user_settings_blacklist').
                              (This field together with 'user_settings_override*'
                              and 'system_settings' defines the total set of resource
                              settings)
                            type: string
                          userSettingsOverrideJSON:
                            description: An arbitrary JSON object allowing ECE admins
                              owners to set clusters' parameters (only one of this
                              and 'user_settings_override_yaml' is allowed), ie in
                              addition to the documented 'system_settings'. (This
                              field together with 'system_settings' and 'user_settings*'
                              defines the total set of resource settings)
                            type: string
                          userSettingsOverrideYaml:
                            description: An arbitrary YAML object allowing (non-admin)
                              cluster owners to set their parameters (only one of
                              this and 'user_settings_json' is allowed), provided
                              they are on the whitelist ('user_settings_whitelist')
                              and not on the blacklist ('user_settings_blacklist').
                              (These field together with 'user_settings_override*'
                              and 'system_settings' defines the total set of resource
                              settings)
                            type: string
                          userSettingsYaml:
                            description: An arbitrary YAML object allowing ECE admins
                              owners to set clusters' parameters (only one of this
                              and 'user_settings_override_json' is allowed), ie in
                              addition to the documented 'system_settings'. (This
                              field together with 'system_settings' and 'user_settings*'
                              defines the total set of resource settings)
                            type: string
                        type: object
                      elasticsearchClusterRefID:
                        type: string
                      httpEndpoint:
                        type: string
                      httpsEndpoint:
                        type: string
                      refID:
                        type: string
                      region:
                        type: string
                      resourceID:
                        type: string
                      topology:
                        items:
                          properties:
                            instanceConfigurationID:
                              type: string
                            size:
                              type: string
                            sizeResource:
                              description: Optional size type, defaults to "memory".
                              type: string
                            zoneCount:
                              format: int64
                              type: integer
                          type: object
                        type: array
                    type: object
                  name:
                    description: Optional name for the deployment
                    type: string
                  observability:
                    description: Optional observability settings. Ship logs and metrics
                      to a dedicated deployment.
                    properties:
                      deploymentID:
                        type: string
                      logs:
                        type: boolean
                      metrics:
                        type: boolean
                      refID:
                        type: string
                    required:
                    - deploymentID
                    type: object
                  region:
                    description: Required ESS region where to create the deployment,
                      for ECE environments "ece-region" must be set
                    type: string
                  requestID:
                    description: Optional request_id to set on the create operation,
                      only use when previous create attempts return with an error
                      and a request_id is returned as part of the error
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: Optional map of deployment tags
                    type: object
                  timeouts:
                    properties:
                      create:
                        description: A Duration represents the elapsed time between
                          two instants as an int64 nanosecond count. The representation
                          limits the largest representable duration to approximately
                          290 years.
                        format: int64
                        type: integer
                      default:
                        description: A Duration represents the elapsed time between
                          two instants as an int64 nanosecond count. The representation
                          limits the largest representable duration to approximately
                          290 years.
                        format: int64
                        type: integer
                      delete:
                        description: A Duration represents the elapsed time between
                          two instants as an int64 nanosecond count. The representation
                          limits the largest representable duration to approximately
                          290 years.
                        format: int64
                        type: integer
                      read:
                        description: A Duration represents the elapsed time between
                          two instants as an int64 nanosecond count. The representation
                          limits the largest representable duration to approximately
                          290 years.
                        format: int64
                        type: integer
                      update:
                        description: A Duration represents the elapsed time between
                          two instants as an int64 nanosecond count. The representation
                          limits the largest representable duration to approximately
                          290 years.
                        format: int64
                        type: integer
                    type: object
                  trafficFilter:
                    description: Optional list of traffic filters to apply to this
                      deployment.
                    items:
                      type: string
                    minItems: 1
                    type: array
                  version:
                    description: Required Elastic Stack version to use for all of
                      the deployment resources
                    type: string
                required:
                - deploymentTemplateID
                - elasticsearch
                - region
                - version
                type: object
              secretRef:
                description: LocalObjectReference contains enough information to let
                  you locate the referenced object inside the same namespace.
                properties:
                  name:
                    description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      TODO: Add other useful fields. apiVersion, kind, uid?'
                    type: string
                type: object
              state:
                properties:
                  alias:
                    description: Optional deployment alias that affects the format
                      of the resource URLs
                    type: string
                  apm:
                    description: Optional APM resource definition
                    properties:
                      config:
                        description: Optionally define the Apm configuration options
                          for the APM Server
                        properties:
                          debugEnabled:
                            description: Optionally enable debug mode for APM servers
                              - defaults to false
                            type: boolean
                          userSettingsJSON:
                            description: An arbitrary JSON object allowing (non-admin)
                              cluster owners to set their parameters (only one of
                              this and 'user_settings_yaml' is allowed), provided
                              they are on the whitelist ('user_settings_whitelist')
                              and not on the blacklist ('user_settings_blacklist').
                              (This field together with 'user_settings_override*'
                              and 'system_settings' defines the total set of resource
                              settings)
                            type: string
                          userSettingsOverrideJSON:
                            description: An arbitrary JSON object allowing ECE admins
                              owners to set clusters' parameters (only one of this
                              and 'user_settings_override_yaml' is allowed), ie in
                              addition to the documented 'system_settings'. (This
                              field together with 'system_settings' and 'user_settings*'
                              defines the total set of resource settings)
                            type: string
                          userSettingsOverrideYaml:
                            description: An arbitrary YAML object allowing (non-admin)
                              cluster owners to set their parameters (only one of
                              this and 'user_settings_json' is allowed), provided
                              they are on the whitelist ('user_settings_whitelist')
                              and not on the blacklist ('user_settings_blacklist').
                              (These field together with 'user_settings_override*'
                              and 'system_settings' defines the total set of resource
                              settings)
                            type: string
                          userSettingsYaml:
                            description: An arbitrary YAML object allowing ECE admins
                              owners to set clusters' parameters (only one of this
                              and 'user_settings_override_json' is allowed), ie in
                              addition to the documented 'system_settings'. (This
                              field together with 'system_settings' and 'user_settings*'
                              defines the total set of resource settings)
                            type: string
                        type: object
                      elasticsearchClusterRefID:
                        type: string
                      httpEndpoint:
                        type: string
                      httpsEndpoint:
                        type: string
                      refID:
                        type: string
                      region:
                        type: string
                      resourceID:
                        type: string
                      topology:
                        items:
                          properties:
                            instanceConfigurationID:
                              type: string
                            size:
                              type: string
                            sizeResource:
                              description: Optional size type, defaults to "memory".
                              type: string
                            zoneCount:
                              format: int64
                              type: integer
                          type: object
                        type: array
                    type: object
                  deploymentTemplateID:
                    description: Required Deployment Template identifier to create
                      the deployment from
                    type: string
                  elasticsearch:
                    description: Required Elasticsearch resource definition
                    properties:
                      autoscale:
                        description: Enable or disable autoscaling. Defaults to the
                          setting coming from the deployment template. Accepted values
                          are "true" or "false".
                        type: string
                      cloudID:
                        description: The encoded Elasticsearch credentials to use
                          in Beats or Logstash
                        type: string
                      config:
                        description: Optional Elasticsearch settings which will be
                          applied to all topologies unless overridden on the topology
                          element
                        properties:
                          plugins:
                            description: List of Elasticsearch supported plugins,
                              which vary from version to version. Check the Stack
                              Pack version to see which plugins are supported for
                              each version. This is currently only available from
                              the UI and [ecctl](https://www.elastic.co/guide/en/ecctl/master/ecctl_stack_list.html)
                            items:
                              type: string
                            minItems: 1
                            type: array
                          userSettingsJSON:
                            description: JSON-formatted user level "elasticsearch.yml"
                              setting overrides
                            type: string
                          userSettingsOverrideJSON:
                            description: JSON-formatted admin (ECE) level "elasticsearch.yml"
                              setting overrides
                            type: string
                          userSettingsOverrideYaml:
                            description: YAML-formatted admin (ECE) level "elasticsearch.yml"
                              setting overrides
                            type: string
                          userSettingsYaml:
                            description: YAML-formatted user level "elasticsearch.yml"
                              setting overrides
                            type: string
                        type: object
                      extension:
                        description: Optional Elasticsearch extensions such as custom
                          bundles or plugins.
                        items:
                          properties:
                            name:
                              description: Extension name.
                              type: string
                            type:
                              description: Extension type, only `bundle` or `plugin`
                                are supported.
                              type: string
                            url:
                              description: Bundle or plugin URL, the extension URL
                                can be obtained from the `ec_deployment_extension.<name>.url`
                                attribute or the API and cannot be a random HTTP address
                                that is hosted elsewhere.
                              type: string
                            version:
                              description: Elasticsearch compatibility version. Bundles
                                should specify major or minor versions with wildcards,
                                such as `7.*` or `*` but **plugins must use full version
                                notation down to the patch level**, such as `7.10.1`
                                and wildcards are not allowed.
                              type: string
                          required:
                          - name
                          - type
                          - url
                          - version
                          type: object
                        minItems: 1
                        type: array
                      httpEndpoint:
                        description: The Elasticsearch resource HTTP endpoint
                        type: string
                      httpsEndpoint:
                        description: The Elasticsearch resource HTTPs endpoint
                        type: string
                      refID:
                        description: Optional ref_id to set on the Elasticsearch resource
                        type: string
                      region:
                        description: The Elasticsearch resource region
                        type: string
                      remoteCluster:
                        description: Optional Elasticsearch remote clusters to configure
                          for the Elasticsearch resource, can be set multiple times
                        items:
                          properties:
                            alias:
                              description: Alias for this Cross Cluster Search binding
                              type: string
                            deploymentID:
                              description: Remote deployment ID
                              type: string
                            refID:
                              description: Remote elasticsearch "ref_id", it is best
                                left to the default value
                              type: string
                            skipUnavailable:
                              description: If true, skip the cluster during search
                                when disconnected
                              type: boolean
                          required:
                          - deploymentID
                          type: object
                        minItems: 1
                        type: array
                      resourceID:
                        description: The Elasticsearch resource unique identifier
                        type: string
                      snapshotSource:
                        description: Optional snapshot source settings. Restore data
                          from a snapshot of another deployment.
                        properties:
                          snapshotName:
                            description: Name of the snapshot to restore. Use '__latest_success__'
                              to get the most recent successful snapshot.
                            type: string
                          sourceElasticsearchClusterID:
                            description: ID of the Elasticsearch cluster that will
                              be used as the source of the snapshot
                            type: string
                        required:
                        - sourceElasticsearchClusterID
                        type: object
                      topology:
                        description: Optional topology element which must be set once
                          but can be set multiple times to compose complex topologies
                        items:
                          properties:
                            ID:
                              description: Required topology ID from the deployment
                                template
                              type: string
                            autoscaling:
                              description: Optional Elasticsearch autoscaling settings,
                                such a maximum and minimum size and resources.
                              properties:
                                maxSize:
                                  description: Maximum size value for the maximum
                                    autoscaling setting.
                                  type: string
                                maxSizeResource:
                                  description: Maximum resource type for the maximum
                                    autoscaling setting.
                                  type: string
                                minSize:
                                  description: Minimum size value for the minimum
                                    autoscaling setting.
                                  type: string
                                minSizeResource:
                                  description: Minimum resource type for the minimum
                                    autoscaling setting.
                                  type: string
                                policyOverrideJSON:
                                  description: Computed policy overrides set directly
                                    via the API or other clients.
                                  type: string
                              type: object
                            instanceConfigurationID:
                              description: Computed Instance Configuration ID of the
                                topology element
                              type: string
                            nodeRoles:
                              description: The computed list of node roles for the
                                current topology element
                              items:
                                type: string
                              type: array
                            nodeTypeData:
                              description: The node type for the Elasticsearch Topology
                                element (data node)
                              type: string
                            nodeTypeIngest:
                              description: The node type for the Elasticsearch Topology
                                element (ingest node)
                              type: string
                            nodeTypeMaster:
                              description: The node type for the Elasticsearch Topology
                                element (master node)
                              type: string
                            nodeTypeMl:
                              description: The node type for the Elasticsearch Topology
                                element (machine learning node)
                              type: string
                            size:
                              description: Optional amount of memory per node in the
                                "<size in GB>g" notation
                              type: string
                            sizeResource:
                              description: Optional size type, defaults to "memory".
                              type: string
                            zoneCount:
                              description: Optional number of zones that the Elasticsearch
                                cluster will span. This is used to set HA
                              format: int64
                              type: integer
                          required:
                          - ID
                          type: object
                        minItems: 1
                        type: array
                      trustAccount:
                        description: Optional Elasticsearch account trust settings.
                        items:
                          properties:
                            accountID:
                              description: The ID of the Account.
                              type: string
                            trustAll:
                              description: If true, all clusters in this account will
                                by default be trusted and the `trust_allowlist` is
                                ignored.
                              type: boolean
                            trustAllowlist:
                              description: The list of clusters to trust. Only used
                                when `trust_all` is false.
                              items:
                                type: string
                              type: array
                          required:
                          - accountID
                          - trustAll
                          type: object
                        type: array
                      trustExternal:
                        description: Optional Elasticsearch external trust settings.
                        items:
                          properties:
                            relationshipID:
                              description: The ID of the external trust relationship.
                              type: string
                            trustAll:
                              description: If true, all clusters in this account will
                                by default be trusted and the `trust_allowlist` is
                                ignored.
                              type: boolean
                            trustAllowlist:
                              description: The list of clusters to trust. Only used
                                when `trust_all` is false.
                              items:
                                type: string
                              type: array
                          required:
                          - relationshipID
                          - trustAll
                          type: object
                        type: array
                    type: object
                  elasticsearchUsername:
                    description: Computed username obtained upon creating the Elasticsearch
                      resource
                    type: string
                  enterpriseSearch:
                    description: Optional Enterprise Search resource definition
                    properties:
                      config:
                        description: Optionally define the Enterprise Search configuration
                          options for the Enterprise Search Server
                        properties:
                          userSettingsJSON:
                            description: An arbitrary JSON object allowing (non-admin)
                              cluster owners to set their parameters (only one of
                              this and 'user_settings_yaml' is allowed), provided
                              they are on the whitelist ('user_settings_whitelist')
                              and not on the blacklist ('user_settings_blacklist').
                              (This field together with 'user_settings_override*'
                              and 'system_settings' defines the total set of resource
                              settings)
                            type: string
                          userSettingsOverrideJSON:
                            description: An arbitrary JSON object allowing ECE admins
                              owners to set clusters' parameters (only one of this
                              and 'user_settings_override_yaml' is allowed), ie in
                              addition to the documented 'system_settings'. (This
                              field together with 'system_settings' and 'user_settings*'
                              defines the total set of resource settings)
                            type: string
                          userSettingsOverrideYaml:
                            description: An arbitrary YAML object allowing (non-admin)
                              cluster owners to set their parameters (only one of
                              this and 'user_settings_json' is allowed), provided
                              they are on the whitelist ('user_settings_whitelist')
                              and not on the blacklist ('user_settings_blacklist').
                              (These field together with 'user_settings_override*'
                              and 'system_settings' defines the total set of resource
                              settings)
                            type: string
                          userSettingsYaml:
                            description: An arbitrary YAML object allowing ECE admins
                              owners to set clusters' parameters (only one of this
                              and 'user_settings_override_json' is allowed), ie in
                              addition to the documented 'system_settings'. (This
                              field together with 'system_settings' and 'user_settings*'
                              defines the total set of resource settings)
                            type: string
                        type: object
                      elasticsearchClusterRefID:
                        type: string
                      httpEndpoint:
                        type: string
                      httpsEndpoint:
                        type: string
                      refID:
                        type: string
                      region:
                        type: string
                      resourceID:
                        type: string
                      topology:
                        items:
                          properties:
                            instanceConfigurationID:
                              type: string
                            nodeTypeAppserver:
                              type: boolean
                            nodeTypeConnector:
                              type: boolean
                            nodeTypeWorker:
                              type: boolean
                            size:
                              type: string
                            sizeResource:
                              description: Optional size type, defaults to "memory".
                              type: string
                            zoneCount:
                              format: int64
                              type: integer
                          type: object
                        type: array
                    type: object
                  id:
                    type: string
                  kibana:
                    description: Optional Kibana resource definition
                    properties:
                      config:
                        description: Optionally define the Kibana configuration options
                          for the Kibana Server
                        properties:
                          userSettingsJSON:
                            description: An arbitrary JSON object allowing (non-admin)
                              cluster owners to set their parameters (only one of
                              this and 'user_settings_yaml' is allowed), provided
                              they are on the whitelist ('user_settings_whitelist')
                              and not on the blacklist ('user_settings_blacklist').
                              (This field together with 'user_settings_override*'
                              and 'system_settings' defines the total set of resource
                              settings)
                            type: string
                          userSettingsOverrideJSON:
                            description: An arbitrary JSON object allowing ECE admins
                              owners to set clusters' parameters (only one of this
                              and 'user_settings_override_yaml' is allowed), ie in
                              addition to the documented 'system_settings'. (This
                              field together with 'system_settings' and 'user_settings*'
                              defines the total set of resource settings)
                            type: string
                          userSettingsOverrideYaml:
                            description: An arbitrary YAML object allowing (non-admin)
                              cluster owners to set their parameters (only one of
                              this and 'user_settings_json' is allowed), provided
                              they are on the whitelist ('user_settings_whitelist')
                              and not on the blacklist ('user_settings_blacklist').
                              (These field together with 'user_settings_override*'
                              and 'system_settings' defines the total set of resource
                              settings)
                            type: string
                          userSettingsYaml:
                            description: An arbitrary YAML object allowing ECE admins
                              owners to set clusters' parameters (only one of this
                              and 'user_settings_override_json' is allowed), ie in
                              addition to the documented 'system_settings'. (This
                              field together with 'system_settings' and 'user_settings*'
                              defines the total set of resource settings)
                            type: string
                        type: object
                      elasticsearchClusterRefID:
                        type: string
                      httpEndpoint:
                        type: string
                      httpsEndpoint:
                        type: string
                      refID:
                        type: string
                      region:
                        type: string
                      resourceID:
                        type: string
                      topology:
                        items:
                          properties:
                            instanceConfigurationID:
                              type: string
                            size:
                              type: string
                            sizeResource:
                              description: Optional size type, defaults to "memory".
                              type: string
                            zoneCount:
                              format: int64
                              type: integer
                          type: object
                        type: array
                    type: object
                  name:
                    description: Optional name for the deployment
                    type: string
                  observability:
                    description: Optional observability settings. Ship logs and metrics
                      to a dedicated deployment.
                    properties:
                      deploymentID:
                        type: string
                      logs:
                        type: boolean
                      metrics:
                        type: boolean
                      refID:
                        type: string
                    required:
                    - deploymentID
                    type: object
                  region:
                    description: Required ESS region where to create the deployment,
                      for ECE environments "ece-region" must be set
                    type: string
                  requestID:
                    description: Optional request_id to set on the create operation,
                      only use when previous create attempts return with an error
                      and a request_id is returned as part of the error
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: Optional map of deployment tags
                    type: object
                  timeouts:
                    properties:
                      create:
                        description: A Duration represents the elapsed time between
                          two instants as an int64 nanosecond count. The representation
                          limits the largest representable duration to approximately
                          290 years.
                        format: int64
                        type: integer
                      default:
                        description: A Duration represents the elapsed time between
                          two instants as an int64 nanosecond count. The representation
                          limits the largest representable duration to approximately
                          290 years.
                        format: int64
                        type: integer
                      delete:
                        description: A Duration represents the elapsed time between
                          two instants as an int64 nanosecond count. The representation
                          limits the largest representable duration to approximately
                          290 years.
                        format: int64
                        type: integer
                      read:
                        description: A Duration represents the elapsed time between
                          two instants as an int64 nanosecond count. The representation
                          limits the largest representable duration to approximately
                          290 years.
                        format: int64
                        type: integer
                      update:
                        description: A Duration represents the elapsed time between
                          two instants as an int64 nanosecond count. The representation
                          limits the largest representable duration to approximately
                          290 years.
                        format: int64
                        type: integer
                    type: object
                  trafficFilter:
                    description: Optional list of traffic filters to apply to this
                      deployment.
                    items:
                      type: string
                    minItems: 1
                    type: array
                  version:
                    description: Required Elastic Stack version to use for all of
                      the deployment resources
                    type: string
                required:
                - deploymentTemplateID
                - elasticsearch
                - region
                - version
                type: object
              terminationPolicy:
                enum:
                - Delete
                - DoNotTerminate
                type: string
              updatePolicy:
                enum:
                - Destroy
                - DoNotDestroy
                type: string
            required:
            - providerRef
            - resource
            type: object
          status:
            properties:
              conditions:
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another. This should be when the underlying condition changed.  If
                        that is not known, then using the time when the API field
                        changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: A human readable message indicating details about
                        the transition. This field may be empty.
                      type: string
                    observedGeneration:
                      description: If set, this represents the .metadata.generation
                        that the condition was set based upon. For instance, if .metadata.generation
                        is currently 12, but the .status.condition[x].observedGeneration
                        is 9, the condition is out of date with respect to the current
                        state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: The reason for the condition's last transition
                        in CamelCase. The specific API may choose whether or not this
                        field is considered a guaranteed API. This field may not be
                        empty.
                      type: string
                    status:
                      description: Status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: Type of condition in CamelCase or in foo.example.com/CamelCase.
                        Many .condition.type values are consistent across resources
                        like Available, but because arbitrary conditions can be useful
                        (see .node.status.conditions), the ability to deconflict is
                        important.
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: Resource generation, which is updated on mutation by
                  the API Server.
                format: int64
                type: integer
              phase:
                description: Status defines the set of statuses a resource can have.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
